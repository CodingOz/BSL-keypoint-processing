# BSL-keypoint-processing
Past repository accidentally contained an encrypted survey sign recording which has special category personal data (race and gender). Git version history retains all committed data regardless of subsequent deletions so new repository used to stay in line with GDPR obligations and the principle of data minimisation.
